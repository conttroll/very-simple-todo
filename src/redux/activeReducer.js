const initialState = {
    activeTodos: []
}

const activeReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default activeReducer;